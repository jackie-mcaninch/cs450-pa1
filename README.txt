To build file enter "make" in terminal, then enter "./run" on the next line
To do both at once enter "make test"
If both fail, run manually with "gcc -Wall lab.c -o run" followed by "./run" on next line
